# Local runtime state placeholder

Real local state lives under `~/aios/vault/ops/state/aios-kit` by default and is ignored here.

This directory exists only so the public repo documents the expected shape without committing machine-specific runtime state.
